# genios-brain — Plans Index

Repo-local plans. The **master index across the whole workspace** is `../../../docs/plans/README.md`,
which lists these files too (via `../../genios-brain/docs/plans/…` links). Keep both in step.

Convention (from the workspace `CLAUDE.md`, mandatory): every plan carries a header block —
`> **Created:** YYYY-MM-DD · **Status:** Active | Post-trial | Reference | Done` plus a one-line
**Purpose** — and a row here. When a plan is completed or superseded: update its Status, move the
file to `docs/_archive/plans/`, and update its row.

*Last audited against code: 2026-08-30 (HEAD `0eaf94e`).*

| Plan | Created | Status | What it is, and what is actually left |
|---|---|---|---|
| [CREDITS_AND_SYNC_METERING.md](CREDITS_AND_SYNC_METERING.md) | 2026-09-10 | 🔴 Active — built; two pricing decisions open | **What a credit is, where it is charged, and why sync is not charged in credits.** Counted off the code rather than assumed: sync is **~0.35 LLM calls per captured email**, not 4 — the junk gate batches 12/call, ESQE 20/call, L1 semantic is dark in prod, and L2 extraction runs only on the ~27% that pass the gate. But the cost lands the other way: the L2 prompt is 2,809 tokens, so **one email costs ₹0.16 to ingest against a query's ₹0.12 — about 1.3 credits.** Charged to the credit pool, a 3,000-message mailbox would burn ~4,000 credits **before the product answered anything**, and would bill the customer for how much mail other people send them. Hence **two meters**: credits for intelligence the user clicks, and a per-period message count for ingestion (`platform/quota.py`, migration 0128), trimmed rather than refused and failing open. Plans: free 3,000 / Individual 10,000 / Startup 100,000 / Growth 300,000 credits, with seats 1/1/5/15 and domains 1/1/3/10 now enforced. §5 prices the liability — Startup at ₹25,000 is 20% margin at full burn — and finds the unpulled lever: `build_prompt` is **99% identical between any two emails but puts the message in the MIDDLE**, so only 1,260 of 11,238 chars are a cacheable prefix; moving it to the end is worth ~30% (output, not input, is the larger half). §6 is what still needs an owner: Growth has no INR price and **the pricing page's $99 does not support Startup's 100,000 credits**. |
| [DELIVERY_BILLING_READINESS.md](DELIVERY_BILLING_READINESS.md) | 2026-09-09 | 🟢 Active — built and proven, deployment pending | **Signup → login → trial → credits → deduct → expiry → upgrade/top-up, audited and then CLOSED.** Thirteen defects found and fixed. The tier vocabulary was defined four times and the four disagreed — `early` (a sellable ₹4,500/mo plan) was **missing from the seat and credit tables** so it fell to a 100-credit default, and `startup` read 100,000 credits in one file and 2,000 in another; there is one `PLANS` table now and a contract test fails if a route grows a rival. The trial granted 10,000 credits but capped 200 queries/day over 15 days, so **7,000 were unreachable** — the daily ceiling is derived from the allowance now (`ceil(credits/period) × 3`). **Nothing expired a trial or a plan**: no code wrote `plan_status='expired'` or set `grace_until`, making the dashboard's expiry banner and `in_grace` unreachable UI — `run_billing_tick` rides the in-process heartbeat (no Celery: Upstash quota) and `refusal_for` gates query/analyze/draft, refusing on the PLAN and the WALLET with different messages. `/v1/intelligence/analyze` — the extension's main surface, Sonnet when `deep` — **charged nothing**; prices are query 1 · analyze 1 · analyze-deep 4 · draft 2, one credit ≈ one Haiku synthesis at list (≈₹0.12). Both payment webhooks were `return {"ack": True}` **stubs**, so a browser closed mid-payment charged the customer and granted nothing; there is one `_fulfil` path now, shared by the browser return and the webhook and idempotent on `payment_id`. Two security fixes fell out: **a signed order could redeem any top-up pack** (the pack came from the caller — the repo's own test did this and passed), and the webhook now reads the org from the paid row so a leaked secret cannot grant across tenants. Also: **USD checkout never existed** (only Razorpay orders were ever built — refused at the door until Stripe is real), `/usage` counted `decisions` rows instead of the ledger, ingestion inherited the paying tenant's $25/day cap on free trials (now $3/day), and the ₹2,500 top-up pack **advertised itself at ₹250,000**. §5 is the ten-step proof, run green on a scratch Postgres. |
| [EXPECTED_VS_REALITY.md](EXPECTED_VS_REALITY.md) · [page](EXPECTED_VS_REALITY.html) | 2026-09-08 | 🔴 Active — the design doc vs the shipped engine | **`GeniOS-Design-Globe` (expected) put side by side with the code at `acb18c8` and the measured pilot (reality), layer by layer, group by group.** Answers why the first live run produced 9 cards of which 3 are one marketing email in the founder's voice: (1) L1 has **no audience-fan-out feature** — `To`/`Cc` are captured as `source_events.recipients` and read by no gate, scorer or situation rule, so a mail to one person and a mail to four hundred are the same message; (2) `l2.outreach.read_overdue_commitments` never reads the `owns` edge the graph correctly wrote, and `general_v1`'s imperative template then renders somebody else's promise as the founder's; (3) `verified_evidence_required` can never be satisfied by an **absence**, so 114 `awaiting_response`/`first_response_overdue` situations are held for ever; (4) the **Admin corpus is not substrate-aligned** — 201 capability files reference **20 planned-only fact paths and 20 planned-only situation types** against Sales' **zero**, so the one activated domain is structurally silent and sales plays top the ranking. §8 is the failing-unit register (10 units, each with size and fix), §9 scores the Globe's 15 Admin surfaces as measured (**six promised on two connectors, one and a half shipped**), §11 lists five corrections the Globe itself needs. |
| [L1_L4_PILOT_FUNNEL.md](L1_L4_PILOT_FUNNEL.md) | 2026-09-08 | 🔴 Active — the first live end-to-end run, measured | **What actually happened when Layers 1–4 were switched on for a real tenant** (Rohit, Gmail + Calendar, 12 Aug – 8 Sep), read out of the tables the sweep itself wrote via the new `scripts/pipeline_funnel_report.py`. The funnel: **849 captured → 225 emitted → 395 signals → 231 situations → 18 admitted → 112 candidates → 9 cards**, with every group inside every layer broken out — each gate decision and reason code, the same decisions per SOURCE (calendar passes 100%, Gmail 27%), signals by type (11 of 14 types, **359/395 carrying a verified span**), floor refusals, the graph, situations by type, the admission gate by REASON and by situation TYPE, L3 packages and their brain slices, L4 outcomes/dispositions/plays, and the nine cards with their scores. §6 ranks the seven losses and §7 is the ordered fix list. **The narrowest point is L2 admission at 8%**: `awaiting_response` (74) and `first_response_overdue` (40) admit **zero** — both are absence-shaped situations whose evidence is that nobody replied, so `verified_evidence_required` holds every one. Second is 159 of 159 attachments unread (OCR off); third is all 349 expertise packages carrying capabilities and **empty behaviour/org-rules/adaptive slices** (L3 J5, already recorded); fourth is 16 of 33 runs deferring under the 4,500 confidence floor. Also notes the corpus mismatch — the `sales` plays produce the highest utilities on a tenant where only `admin` is switched on. |
| [L1_PRODUCTION_READINESS.md](L1_PRODUCTION_READINESS.md) | 2026-09-07 | 🔴 Active — L1 is built, deployed and DARK | **What Layer 1 is actually DOING in production**, verified against the live database rather than the build record. `l1_semantic_activation` is **empty**, so S2 never runs, `qualified_signals` and `qualification_drops` are both **0**, all 355 `l1_extraction_results` rows carry a NULL `profile_id` (legacy L2 writer), and Layer 2 is running on `importance_fallback` — so **any L2/L3 tuning done before activation is tuned against the fallback**. The document lane yields ~1%: 776 attachment events, **767 parked and never drained**, with `GENIOS_ENABLE_OCR=false` and no tesseract/poppler in the buildpack image. **Seven defects found by driving the real path and FIXED here** (8,786 tests green on a clean scratch Postgres, the two wiring fixes mutation-checked): the upload door captured, extracted and scored and then published nothing (new `capture/esqe/finalize.py` — one sequence, two doors); an UNTAGGED upload could never be extracted at all (`direction_unknown` → `DELIBERATE_SOURCES`); every OCR-able attachment was filed as terminally `unsupported` instead of the drainable `ocr_unavailable`, which is why the OCR gap was invisible (+ `scripts/recode_parked_documents.py` to correct the 681 rows already written); **a receipt could not name its page or its section** — new `capture/documents/pages.py` carries a PDF's page boundaries from the joiner to the span, `EvidenceSpan.page`/`.section` are attached at the alignment seam and RECOMPUTED when ALG-08 relocates a quote, and the upload door now chunks by section; **migration 0115** adds the four provenance answers a stored signal could not give (`ingested_at`, `content_hash`, `qualification_reason`, `superseded_by` — all served by `GET /qualification/signals`); plus `l1_sync_runs.started_at` (443 rows with a finish and no start) and a double-defined wiring factory. **Left, and each names why:** P0 activate one org and close G10 (owner decision — it starts model spend), P1 OCR deploy image + drain the 767, table-cell provenance (needs a table-aware extractor), P2 `importance_bp` rename vs finishing L2 composition and first-class actor/business_subject (both owner decisions), P3 Gmail `historyId` deletions (needs the pilot mailbox), P4 the golden corpus (8, not 30) and a real eval harness. §5 flags three untracked, unapplied migrations from the parallel L3 work. |
| [L1_V2_BUILD.md](L1_V2_BUILD.md) | 2026-09-05 | 🟢 Active — schedule; see the build record | Build record for Layer 1 v2 (Hybrid Intelligence Extraction): the W0–W10 wave/gate order, the verified counts (**7 groups / 49 components / 65 unit specs against 100 promised**), and the reuse split — **13 reuse · 3 move · 14 fix · 19 build-new · 12 contracts**, so this is not a rewrite. Records **four sequencing defects** found reviewing the spec against this codebase: G2 asserts an `importance_bp` that W7 builds (move to G7), L1.5.8 must move to W1, L1.5.4/L1.5.8 wave assignments contradict between doc 05 and doc 09, and L1.3.4-U3 speech-to-text has no scheduled connector (descoped to synthetic fixtures). **DB hazards:** the `l2_extraction_results` rename breaks tenant erasure at `account_routes.py:326` (15 refs / 11 files), and four new tables need erasure entries — the cascade half is test-enforced, the `_ORG_SCOPED_TABLES` half is not. §5.3 **closed**: `scripts/_db.py` + `tests/test_scripts_db_guard.py`. |
| [L1_V2_BUILD_RECORD.md](L1_V2_BUILD_RECORD.md) | 2026-09-06 | 🟢 Active — G0–G9 met, G10 open | **What Layer 1 v2 actually is in code**, unit by unit: the four doctrines every module obeys (model describes never scores · integer basis points · no claim without a receipt · no clocks in logic), then package by package — `contracts/` (12 typed objects + V-1…V-7), S1 `structural/` `documents/` `structured/` `parked/`, S2 `semantic/` (the sink built before the extractor that fills it), S3 `validate/` (ALG-08 spans, ALG-09 dates, ALG-10 money, ALG-12 conflict, Rule 11), S4 `esqe/` (**ALG-17 importance — the Layer 4 unlock**, measured at 100 distinct values and a 1520 bp p90−p50 spread on the production path, where 193 of 223 signals once shared one score). **6,693 tests passing on real Postgres, 0 skipped.** Records what is NOT done: **G10 needs a real tenant and seven days** — activation row, shadow-diff runner and the old L2 path are all in place, with the exact commands in §5.1; speech-to-text descoped for want of a connector; the golden corpus holds 8 messages, not 30. §6 names the defect this build shipped **six times** — a unit built, tested, and called by nothing on a real request path — and the rule that ends it. |
| [L2_V2_BUILD_RECORD.md](L2_V2_BUILD_RECORD.md) | 2026-09-06 | 🟢 Active — H0–H7 met, H8 open | **What Layer 2 v2 actually is in code**, wave by wave: the analytic stratum (`metric_history` remembers what a metric was in March, so a trend, a cohort position and an anomaly can each be said WITH the numbers behind it), the quality stratum (a typed absence, so "no reply" and "no mailbox connected" stop being one fact), the pattern registry, and a situation engine that COMPOSES an importance instead of stamping 5000 — measured at **213 distinct values over 223 situations, p90−p50 = 2116 bp, 0.89% still at exactly 5000**. **8,323 tests passing on real Postgres, 0 skipped.** Every earlier gate re-measured and none drifted: `graph_facts` **flat at 45 rows across 12 sweeps** (doc 09 item 6, measured not asserted), 0 empty bands at any population with quartiles below eleven, M-4 false positives **0 of 46**, `read_graph(as_of=April)` still returns March after a September sweep. The published evidence is now a span-validated verbatim quote — re-checked here against the stored source text, and a quote the email never contained loses L1's `verified` flag. Records what is NOT done: **H8 needs a real tenant and seven days** (§5.1 has the exact commands), the ADMIT step `L2.5.8-U1` is unbuilt, and — the largest item — **`contracts/situation.py`, the v2 BSO with all eight validators, has ZERO production importers**: the live publisher builds the older dataclass and routes the v2 material through `metadata`, so doc 08's `pattern_id` law has never fired (§5.2, with the reason it is not a quick fix). §6 is the gate's own review: the seven must-not-regress rows violated one at a time, ten X8 mutations, and a reachability closure that found **`may_infer_absent` and `satisfy` had no production caller** — both now wired. |
| [L3_V2_BUILD_RECORD.md](L3_V2_BUILD_RECORD.md) | 2026-09-07 | 🟢 Active — J0–J4 met, J5 six rows of seven | **What Layer 3 v2 actually is in code**, wave by wave (Y0–Y5): the contracts and the per-tenant/per-domain `l3_activation` row; the three typed consumers that make activation mean something (CLG-06 rule compiler, CLG-08 citations, CLG-07 play cap); the Admin corpus; and the two supply-side pipelines that finally FILL the runtime brains — **N-3**, a founder's uploaded policy through `POST /api/org/{org}/upload` → CLG-09 → L6 → human review → Organization brain + `authority_rules`, and **N-4**, L2.4's measured trends → Behavior brain, plus the Adaptive lease a card verdict earns inside its own request. **8,817 tests passing on real Postgres, 0 skipped.** **J4 PASSES on one tenant** and the gate re-measured it rather than reading a report: 5 organization · 6 behavior · 1 live lease, every entry traced to the human who confirmed it, the L6 floors proven load-bearing by re-seeding with `min_confidence_bp` **raised** (6 entries → 0), the lease's expiry driven past its own clock, and Law 3 attacked at the database on INSERT and UPDATE — both refused. **J5 earns six of seven rows** including the two that were previously NOT EARNED: a card carrying a citation (2 of 2, and the quotes re-hashed byte-identical against the authored YAML on disk) and a candidate eliminated by a blocking **Admin** rule. **What is left:** the brain-slice row FAILS, and §5.1 is why it matters — the brains are full and **Layer 3 cannot read them**: `brain_subject_keys` has a reader and no writer, behaviour subjects are node-keyed while situation entities are email-keyed, and the Adaptive lease lives in `temporary_memories` which `PostgresRuntimeBrains` never queries, so `adaptive_preferences` is structurally always empty. §5.3 records the other half of the elimination row honestly: **three Admin blocking rules were already authored and all three are dark** — one is `enforced_by: L5_validation` and the compiler correctly refuses it; the other two are gated on `commitment.due_at`, which `derived.compute_deal_view` reaches a company with **never**, because it assumes the opposite edge direction to the one `pipeline._works_at` writes (the **third** instance of a bug that file's own docstring documents). §5.4: a **percentage** threshold costs the tenant the whole rule, because ALG-10 is a Money parser. §6 carries the round's mutation table (8 mutations, **one survived** — the identity guard — and a test was written for it), the collision resolution (three copies of the L6 admission sequence, now pinned by an AST drift detector), and an explicit **no-tuning diff**: `contracts/learning.py`, `governance.py` and `units.py` are byte-identical to HEAD. |
| [L3_BRAIN_BINDING.md](L3_BRAIN_BINDING.md) | 2026-09-08 | 🟢 Active — Wave A landed and measured; Wave B named, not built | **Closes the defect Layer 3 shipped with: the four brains hold knowledge and cannot bind it to a live situation.** J5's headline row read `packages_with_a_brain_slice = 0` across 4 packages on a tenant holding 5 organization entries, 6 behaviour entries and a live lease — full brains, zero reach. Nine claims re-verified in code first (activation table with **no production caller**; `brain_subject_keys` with two readers and **no writer**; `temporary_memories` never queried, so `adaptive_preferences` was structurally always empty; `orgrule:` subjects matchable by nothing a situation knows; node-keyed behaviour vs email-keyed entities; SQL and Python disagreeing about the same match; `works_at` read against its write direction; a **percentage** threshold costing the whole rule; **zero** readers of `signals.citations` in `deliver/`). **Wave A is one idea, not nine patches:** `contracts/brain_address.py` — a single `kind:value` token vocabulary a situation and a brain entry both speak, selection by set intersection, no wildcards, `orgwide` refused to observational brains, and `legacy_tokens` so pre-contract rows bind with **no backfill**. Plus the writer that resolves email-keyed members to graph nodes, a lease reader filtered on the sweep's **frozen** clock (never `now()`), per-situation live/shadow from `l3_activation` (two compilers on one connection — Admin live and Sales shadow in the same sweep), the both-directions/node-type/`valid_to` fix to `compute_deal_view`, `contracts/units.Ratio` so 15% is 1500 basis points in its own column, and the authored quote reaching the card's `why` verbatim. **Two defects were found only by driving the fixed path**, each hidden behind the one before it: every L6-published entry **crashed the row mapper** (`derived_from` is a list one side and a str the other) and `shadow_compile` swallowed it per situation, losing the **whole package**; and putting the address in the situation's content hash re-minted a package every sweep — `worst_addresses_per_situation` read **3**, which is the 995 MB read-only incident's exact mechanism. **Measured:** brain slice **0 → 2**, `cards_quoting_the_claim_in_their_own_copy` **0 → 2**, churn back to **1**, J5 `passed: true` on 8 of 8 checks, with a **control** proving a pattern about an unrelated person still does not bind and every applied entry naming the token that selected it. **9,629 passed · 152 xfailed · 0 skipped · 0 failed** on a pristine Postgres — the zero-skip proof the build record said did not exist. Migration `0127` is additive with no backfill. **Not done, and flagged:** the audit-readiness situation is NOT built (Anisha was an example of the general failure, not a requirement — `audit_support` stays deferred for the reason authored in `deferrals.yaml`, and un-deferring it needs a trigger that is not a guess); the two `BusinessSituationObject` contracts are still two; behaviour binds through the anchor on today's fixtures because `gather_members` returns `[]` there; legacy-segment matches still allowed but now counted; and the pilot-report tests need a dropped-and-recreated scratch database between full runs. §5 lists what must happen outside the code, including a **half-finished merge in the repository index** (28 unmerged paths, no `MERGE_HEAD`) that blocks committing. |
| [L4_V2_BUILD_RECORD.md](L4_V2_BUILD_RECORD.md) | 2026-09-08 | 🟢 Active — suite GREEN (0 failed/0 errors/0 skipped); K0/K1a/K1/K2 met, K4 doctrine only | **What Layer 4 v2 actually is in code**, waves Z0–Z6: the roster woken from six hardcoded units to **twenty through the dormant Unit Selector**; `Finding` canonized with a permanent evidence digest that outlives the 720h TTL; **the ears** — L2's `importance_bp`, which existed under `reason/` only inside a SQL select, is now 2,500 of the 10,000 basis points that decide, the authored corpus priority is demoted from verdict to a 70/30 prior with the divergence recorded, confidence is composed under Rule 11 instead of last-writer-wins, and the confidence floor stops defaulting to zero; **the voice** — the first model call site in `reason/`, behind one gate, with a seven-check gauntlet; and the critique + daily-brief seams. **K1 measured on a 60-decision pilot built through the production path: 120 distinct `final_utility_bp` in one day** (gate 50) on a lane where every card used to score the same, **180/180 candidates recording formula utility and override divergence, 100% computed `do_nothing`, 60/60 byte-identical replay** — and the importance row isolated exactly: every other score component byte-identical across all 60, Δimportance 6,987 × 2500/10000 = Δformula **1,747 to the basis point**. **G7, H5 and K1 hold on one tenant in one run** — the first time the three-layer chain has closed. **The doctrine holds under attack**: a model instructed to say *"DO NOTHING, escalate to legal, cancel the account"* moved **0 of 60 decisions**, was refused by name (`contradicts_decision`, `out_of_scope_entity`), and a re-decide with 50 stored bundles present moved nothing either. **10 of 10 preserve-hard mutations killed; zero unreached units** (thirteen shipped across L1–L3); published unit metrics **87 → 89, purely additive, zero renamed**. **What is NOT done, and it is large:** the suite is **35 failed / 16 errors** against a 0-failure baseline, all 51 from **one** cause — a parallel L2 round landed an unconditional admission gate ahead of the whole of L3/L4 and every L4 fixture predates it (§5.1 names the remedy, `tests/l1_supply.py`, and the one fixture that must NOT get it). **`urgency_bp` moved from a constant 5,000 to a constant 0** on 60/60 — that K1a row is NOT MET; **11 of 15 running units emit one identical output for all 60 situations**; **below-floor DEFERs = 0**, so K1's silence row is NOT MET, and it was reported rather than bought by lowering the floor; K4's fallback rate and golden review are **unmeasured** because no model is reachable here. §6.1 records the **four product defects this gate had to fix before anything ran at all** — the compiled lane was returning **nothing**, three crashes on one root cause (L2 now hands the compiler typed models where the v1 lane carried mappings) plus a **write on the shadow pass** that made the K1a gate command, read-only at the server, report FAIL with all twenty units unreceipted on a tenant whose roster was fully awake. **§12 IS THE FIX-ROUND GATE (2026-09-08 evening) and supersedes the NOT-DONE rows above.** The suite is **9,627 passed / 0 failed / 0 errors / 0 skipped / 152 xfailed**. The decisive finding: **two of the three NOT-MET rows were measuring the fixture, not the engine** — `scripts/l4_pilot_seed.py` holds every input constant except importance (which is exactly what K1 needs), its canned extraction emits a `due_at` key **nothing in the pipeline reads**, and it never runs the derived roll-ups that put a commitment's date on the company node. A second population (`scripts/l4_urgency_floor_pilot.py`, new) fixes that: **`urgency_bp` is now the U5 ladder rung for rung** (10000/9000/7500/6000/4000/2000/500/0, 20 distinct at the authority), and **`core.confidence` returns four distinct values so the 4,500 floor cuts between them — 26 decisions and 28 below-floor DEFERs, each naming its missing field**. The 11-of-15 constant-output row becomes **3 of 18**, and those three (`core.impact`, `core.opportunity`, `core.risk`) stay **NOT MET** — impact is 2,000 of the 10,000 ranking basis points and is constant on 372/372 candidates. **Nothing was bought**: every module constant in every touched file diffed against HEAD is **additions only — zero removed, renamed or revalued**; the floor is still 4,500, the weights are doc 04's exactly, and Rule 11 was **tightened** (R-1's `unattributed` pool no longer buys independence it never claimed). **K1 did not move** — 120 distinct utilities, 180/180 divergence, 60/60 computed do-nothing, **114/114 byte-identical replay including with every R-site force-failed**, importance still isolated to the basis point. The doctrine was re-attacked live: **26 of 26 hostile generations refused by name, 0 rows moved**. **12 of 12 preserve-hard invariants mutation-killed** — including **PH1, which had no test at all**: injecting `from genios_engine.reason.bundle import narrate` into `decision_maker.py` left the whole suite green, so the AST test that catches it was written. **Cluster C is not a duplicate**: one node to one situation everywhere; only the synthetic tenant node carries three rows, one period review per domain. |
| [L4_RANKING_ACTIVATION_FIXES.md](L4_RANKING_ACTIVATION_FIXES.md) | 2026-09-08 | 🟢 Active — P1–P5 landed and measured; P6 needs a live tenant and a model | **Closes `L4_V2_BUILD_RECORD.md` §12.7 and the cross-layer gaps found auditing L1→L4 together.** A PM review raised eight items; seven were real, **six were already the build record's own open list**, and one ("K7 is unreachable on Gmail+Calendar") is wrong for this checkout — `capture/connectors/dispatch.py` registers gmail, gcal, hubspot, notion, gdrive, database and push_ingest, so reach is a per-tenant *connection* question. The value was in the root causes. **Three ranking components were constant because of TWO independent wiring gaps**, either of which alone would have left them flat: `_plays` never passed `impact_bp`/`effort_bp`/`risk_bp`, so every compiled play took the 5,000 dataclass defaults (the defect `legacy_pack.py:286` had already fixed one lane over); and the only path from a measured unit to the score is an authored `{play_id: delta}` map, authored for `play_impact_bp` and `play_risk_reduction_bp` in ONE hand-written native pack each, for `play_success_bp` **nowhere at all**, and never on the compiled lane — so four units ran, measured, and moved the score by exactly zero. New `adapters/play_priors.py` derives all four components from what the corpus actually declares (step `actor` mix for effort; `failure_modes`/`limits`/`status` for risk; `outcomes`/`metrics`/`objects_used`/situation-fit for impact; `done_when` coverage for success), each term carrying a receipt naming the field it read. **Measured on the shipped 228-playbook corpus: impact 1→24 distinct, effort 1→31, risk 1→10, success 1→11**, and the engine's own K1 gate moves **360→835 distinct utilities** over 4,480 candidates. **Calibrated twice — the first cut was wrong**, putting 115 of 228 playbooks on the risk ceiling and clamping the top ~15% of impact, which is the same constant one layer in on exactly the half that decides what shows first. Two defects found only by wiring it: **`play_effort_bp` is dead config** (nothing reads it, so `core.cost` is deliberately absent from `_DELTA_CONSUMERS` and a test pins the absence), and **`cost_unit._step_effort` prices every step as human** — harmless while `effort_bp` was flat, and the moment it is actor-derived it would "correct" a nine-step automated play back up by the full 3,000bp ceiling. Also: **two contradictory definitions of "is L1 scoring here"** answering oppositely on an empty supply (sweep-scoped `any(...)` ⇒ False vs flatness-based `assess_l1_supply` ⇒ True) — now one tenant-scoped fail-strict `select exists`, with the sweep's own reading kept beside it because the two disagreeing is the interesting case; **the cross-layer wave order was not expressible** (`ranking_v2` live on a dark-L1 tenant is the six-weight model permanently reweighing five, console green) — new `CROSS_LAYER_PRECONDITIONS`, reported not enforced, on the console, the write and the sweep counts; **`deal_momentum_risk` moves to `do_nothing_exposure`** with the old spelling surviving as a read-side alias, because the freeze was defensible for stored rows and quietly covered the ongoing emission too; **`core.impact`/`core.opportunity` now name their silences**; and **importance reached neither published score decomposition** — the stored copy wrote `U I S E K C`, the SQL proof wrote `U I R C`, and the largest weight in the engine was in neither, so a card could name every reason except the biggest one. `SCORE_INPUT_KEYS` names it once; importance publishes as `M` on both sides, read from the candidate's stored components (there is no unit metric to coalesce — Layer 2 composes it outside `CANDIDATE_COMPONENTS` so no unit can raise its own card), and an absent component is **omitted, not zeroed**. Plus `scripts/stack_activation.py`: the whole chain for one tenant in one read, reporting the FIRST blocking link rather than five consequences of one cause. **720 passed / 84 skipped / 0 failed in `tests/reason`, 17 new tests.** NOT done: K4 needs a real model, K7 needs a real tenant and seven days, the full `DecisionObject` envelope is deferred with its reason (it needs a `signals` migration for data already in `reasoning_candidates`), and **nothing is committed**. |
| [L2_MISSING_UNIT_SPECS.md](L2_MISSING_UNIT_SPECS.md) | 2026-09-06 | 🟢 Active — specs written, 13 flags open | **The Layer 2 unit specs the plan promises in prose and never wrote.** Six of L2's seven group docs have no `Units` column, so `scripts/unit_ledger.py` read their promise as *unstated* and Layer 2's gap as **2** while six groups sat between 25% and 60% specified — a group that never states its scope reads greener than one that does. Counted properly the promise is **56 units against 33 written**, a real gap of **23**. This file writes **21 missing specs** and completes **6** that existed as a heading with no acceptance criteria, in the plan's own WHAT/WHY/HOW/ACCEPTANCE format with a `WIRED AT` line naming the request path each unit must be reached from — the defect L1 shipped six times. §3 flags **thirteen** contradictions rather than resolving them, led by **A-1**: doc 07's central claim (*`situation_bso.py:39/:235` stamps 5000 on every BSO*) is **stale at HEAD** — the module already reads L1's score and records `importance_source`; what L2.7.4 still owes is composition steps 2-6. Also **A-3** (L2.3's gate greps for no LLM in a group that hosts two LLM sites) and **A-7** (the confidence vector is a float percent, the doctrine says integer bp). The ledger now reads this file's promise table (`PROMISE_SUPPLEMENTS`) and freezes the promise as well as the gap, so deleting a promise breaks the ratchet instead of closing it. **A-9**..**A-13** are the assumptions the analytic and ESQE code was written under and deferred to a record called `spec_gaps` that never existed — the H3 percentile row that asks one function to be two statistics, the quartile slot with no addressable id, the `metric_history` DDL with no row shape for a coverage-false observation, and two places the landed code is ahead of doc 06. `tests/test_spec_deferrals_resolve.py` now fails if a comment defers to a record that does not exist. **Left: the 13 flags are owner decisions, waves for 6 units are `proposed`, and L2.4's own `L2.4.7-U2`/`L2.4.8-U2` stay open.** |
| [MANAGER_CARD.md](MANAGER_CARD.md) | 2026-09-01 | 🟢 Active — diagnosis done | Why the Mac app shows facts instead of advice, proved by opening ONE live production card: the draft at the bottom is good and specific, the headline is a clock, the situation line **invented a 24-hour SLA that exists nowhere**, and a customer-support breach playbook was attached to a VC thread. Two builds — **A** the card leads with the move (highest visible impact), **B** the domain router (fixes the wrong-expert class copy cannot). **Blocked on a product decision:** the two co-founders gave opposite instructions the same day — Admin-only vs Sales-compulsory. |
| [ADMIN_INTELLIGENCE_MD06.md](ADMIN_INTELLIGENCE_MD06.md) | 2026-09-01 | 🟢 Active — analysis only | MD 06 (Admin Intelligence L2→L5) read against shipped code. Most of its DIAGNOSIS is already closed by the four passes on `harsh/mvp`; what is new is its SPECIFICATION. Twelve gaps ranked, with a judgment on each: the Universal Domain Router and corpus replay tests are the highest-leverage, stage history is the largest missing capability, and the entitlement gate is commercial plumbing that should wait for the SKU decision. Nothing built from this file yet. |
| [INTELLIGENCE_REMAINING.md](INTELLIGENCE_REMAINING.md) | 2026-09-01 | 🟢 Active | What is left after the L2→L4 chain work, ranked by effect on a real card rather than by the audit's order. **D** `outreach.objective` writer (in progress) · **E** the three surfaces that bypass the pipeline (morning-brief is literally hardcoded empty) · **F** cohort engine (needs D) · **G** the four brains reaching a decision (deliberately last — `learned_brain_entries` is near-empty, so a consumer changes nothing visible yet). Names what is NOT being done and why. |
| [L2_L4_INTELLIGENCE_CHAIN.md](L2_L4_INTELLIGENCE_CHAIN.md) | 2026-08-31 | 🟢 Active | Why every card read alike, and the three passes that fixed it: facts derived from silence (`context/waiting.py`), situations named after what is HAPPENING (`awaiting_response`, `commitment_overdue`, `meeting_follow_through`), a card vocabulary of 13 slots instead of 7, truthful `missing_fields`, and stale cards that rebuild in place (migration `0077`). Corpus routes 0 unrouted globally. **Left: apply `0076`+`0077` to production, then set `GENIOS_USE_DOMAIN_COMPILER` for one tenant.** `outreach.objective` still has no writer and is the largest remaining lift in card quality. |
| [ADR_RATIFICATION.md](ADR_RATIFICATION.md) | 2026-08-24 | 🟢 **Active — blocked on a human** | Ten drafted ADRs, each with a grounded recommendation. **0 of 10 ticked.** No engineering work moves this; it needs the owner to read and tick. `IMPLEMENTATION_PROGRAM.md` makes ADR-02 and ADR-07 preconditions for its Phase 1, so this is a live blocker, not paperwork. |
| [ADMIN_SUPPORT_WIRING.md](ADMIN_SUPPORT_WIRING.md) | 2026-08-27 | 🟢 Active | Admin + CS wired end to end, then the `deal.status` double-writer, the `expertise_packages` growth that took production read-only, and the meeting lane. **Numbers inside are stale** — routing is now **121 routed / 32 named-pending / 0 silent of 153** (was 85, then 108). **Two items blocked on a human:** lift the production read-only state + apply migration `0076`, then set `GENIOS_USE_DOMAIN_COMPILER`. Several sections have closed outright (the pack-registration test hazard; the "recommended order"). |
| [NEW_BRAIN_CUTOVER.md](NEW_BRAIN_CUTOVER.md) | 2026-08-25 | 🟢 Active | Switch card generation from legacy pack rules to the compiled capability corpus. **The flip has not happened** — `use_domain_compiler` is `False` at `platform/config.py:110` and set in no environment. Most Phase-2 blockers closed (0 hollow, 153/153 admitted, Admin/CS packs exist); two survive: `CardStore.claim_build` ignores card expiry (`deliver/store.py:49,55`), and the deploy-time L2 worker count. |
| [INTELLIGENCE_SURFACES.md](INTELLIGENCE_SURFACES.md) | 2026-08-26 | 🟢 Active — partially shipped | One intelligence, four surfaces. **Item 1 done** (`0075`, `card_builder._surfaces`). **Item 2 half done** — the app filter shipped (`deliver/store.py:310`), **the agent gateway never got its filter** (`deliver/agent_api.py:82`). Item 3 partial (`why_now` covers 3 reason codes). Item 4 needs one real agent round-trip. Item 5 (package surface) not started. |
| [IMPLEMENTATION_PROGRAM.md](IMPLEMENTATION_PROGRAM.md) | 2026-08-23 | 🟢 Active for **six** gaps only | The Secret War register — 106 gaps across 7 layers. Its old status ("awaiting owner approval to start Phase 0/0B") was **false when written**: Phases 0-7 shipped in `944b4f7`, the same commit that added the file. ~97 closed there, L3-09 closed by the corpus programme. **Open: X-07, X-01, L1-13, L5-06b, L3-11, and L7-04 (unverified).** The file holds no closure record of its own — that lives only in `git show 944b4f7`. |
| [L2_L3_GAP_ANALYSIS.md](L2_L3_GAP_ANALYSIS.md) | 2026-08-27 | 🔵 Reference — headline now false | Why a complete Sales corpus changed nothing live. **L2-1 (no `deal` node) is fixed**, Admin/CS have a pack lane, 0 hollow capabilities, Sales routes 43/47. **Still open: L2-2** (11 observation kinds emitted by nothing), **L2-3** (no commitment backfill), **L2-4** (no stage history). |
| [PERFORMANCE_HARDENING.md](PERFORMANCE_HARDENING.md) | 2026-08-11 | 🟢 Active — 2 of 6 open | L1→L4 speed. P1 bulk reads, P2 no-op audit skip, P4 L2 parallel extraction, P5 L1 paging all shipped. **P2b (batch the audit writes) and P3 (skip nodes that cannot fire) are open.** The wall-time and emission numbers in the body have not been re-measured. |
| [INTELLIGENCE_SHALLOWNESS_ROOT_CAUSE.md](INTELLIGENCE_SHALLOWNESS_ROOT_CAUSE.md) | 2026-08-22 | 🔵 Reference | 10-agent root-cause of why suggestions read as "no intelligence". **Substantially remediated** — the central finding (no counterparty, no direction) is fixed in `context/extract/envelope.py` + `prompt.py`; the constant Impact is gone. Read for reasoning, not as a to-do. The `champion_quiet` / `MappingProxyType` finding is **unverified** either way. |
| [WHY_THE_INTELLIGENCE_IS_SHALLOW.md](WHY_THE_INTELLIGENCE_IS_SHALLOW.md) | 2026-09-10 | 🔴 Active — analysis complete, build not started | **One consolidated answer to why a human reading this database writes better intelligence than the product can.** Measured live: of **153 tables, 51 have never had a row written**. Everything running produces BEHAVIOUR (who mailed whom, when, whose turn); everything off or unwired produces MEANING. §1 is the entire contents of all four activation tables — every row written by one person by hand in a 3-second window on 8 Sep; **`bundle`, `critique` and `brief` have never been on for anyone**, and `bundle` gates `reason/llm_sites`' three sites (R-1 interpret, R-3 alternatives, R-4 effect) — **the half of Layer 4 that writes prose has never executed once** (`l4_reasoning_bundles`, `l4_r_site_calls`, `l4_r_site_generations`, `l4_brief_rankings` all 0). §3 measures the graph against the model it should have: 43/43 companies have people and 49/60 people have a company, but only **9 of 60 have a commitment and 6 of 60 a role** — and of 86 non-derived fact fields the ones carrying business meaning read `party.role` **1**, `relationship.nature` **1**, `deal.stage`/`person.title`/`thread.objective`/`campaign.objective` **0**. It is a behavioural graph, not a business graph, so the best sentence available about Theresa is "hasn't replied in 30 days" no matter how good the plumbing gets. §4 is the nine plumbing faults with measurements (evidence counted on nodes that are always empty; `_write_fact` writing no source refs — company 509 facts/**0** refs; the calendar lane writing 29 facts and **0** observations; 30 of 60 people carrying no evidence at all; 80 open merge proposals holding identity at 40; the `analytic` axis vetoing every tenant under **78 peers**). §5 locates every LLM call — 4,150 in L1/L2/L5 and **zero** in L4. §6 is four tracks: **A** plumbing (cards appear, 6→26), **B** give the graph nouns (the track that closes the gap to a human), **C** switch on Layer 4's writing half, **D** close the learning loop. §7 orders them with the number that proves each. |
| [INTELLIGENCE_PARITY_PLAN.md](INTELLIGENCE_PARITY_PLAN.md) | 2026-09-10 | 🔴 Active — diagnosis measured, build in progress | **Why the compiled lane has produced ZERO cards in its life, and the ordered route out.** Measured read-only on the live tenant: 18 cards ever, both kinds legacy (`unanswered_email` 7, `commitment_overdue` 11); cards carrying a `capability_id`: **0**. The chain dies in one place — **Layer 2 counts a situation's evidence as `graph_observations` on its own anchor, and observations are written only onto the person who SENT an email**, so every synthetic anchor (campaign 2, organization 2, outreach 41, thread 66, company 43) scores zero, the L2 ceiling lands at 2300 and the 4500 floor defers 53 of 53 runs. Underneath it, **derived facts carry no provenance at all** — company 509 facts / **0** source refs, outreach 175/0, campaign 12/0, organization 10/0 — because `_write_fact` inserts into `graph_facts` alone; restore those refs and campaign/organization/outreach evidence goes **0 → 65** against a 45 bar. Also measured: **30 of 60 person nodes are completely empty** and they are exactly the people who never replied (398 of 1001 observations sit on the founder's own node); **80 open merge proposals** hold `identity` at 40 across the tenant (worth **+9** situations, no code); **zero people appear in both gmail and gcal**, so the 60-point corroboration half of `evidence_score` is unreachable; and the `analytic` axis is a **wall, not a measurement** — its ramp puts every legal cohort in [5,77] peers below the floor, so a small tenant is vetoed forever (one-line fix). §4 is explicit that **the floor stays at 4500** and every fix raises the INPUT. §5 ranks the eight changes by measured effect ÷ effort with the number that proves each. The acceptance test is not a unit test: run the live pipeline and get a `campaign_awaiting_reply` card — **whose copy is already authored** and has simply never been reachable. |
| [INTELLIGENCE_QA_ROHIT_AUDIT.md](INTELLIGENCE_QA_ROHIT_AUDIT.md) | 2026-08-12 | 🔵 Reference | 4-agent QA audit against the design partner's real org. RC1/RC2/RC3 closed; RC4 and RC5 partial. What survives is owned elsewhere: the commitment backfill (`L2_L3_GAP_ANALYSIS.md` L2-3) and the reasoning-write batching (`PERFORMANCE_HARDENING.md` P2b). |

**Status legend:** 🟢 Active (work pending) · 🟡 Post-trial (on hold) · 🔵 Reference (read-only; anything still open is named in the row) · ✅ Done → move to `docs/_archive/plans/`.

## Archived

| Plan | Archived | Why |
|---|---|---|
| [ONBOARDING_SYNC_PROGRESS.md](../_archive/plans/ONBOARDING_SYNC_PROGRESS.md) | 2026-08-30 | All four parts shipped — backfill ceilings raised (`api/routes.py:1104,1106`), `onboarding_progress` + `platform/progress.py` + `home_routes.py:137`, the dashboard `<SyncProgress>` poller, and the Anthropic/Composio timeouts. The plan's prod phase-by-phase observation was not re-run. |
