# speedrun008

Working folder for the `speedrun008` branch (branched off `y-combinator-w27`,
fast-forwarded onto `origin/harsh/mvp` @ f905b65e).

Non-product files for this branch live here. Product code stays in
`genios_engine/`, `migrations/`, `tests/`.

Subfolders get made when there is something to put in them.
