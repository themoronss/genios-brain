"""Signal Layer — derived, typed, provenance-linked values about graph nodes.

A signal is the seam between raw comms/graph and the reasoning engine: rules
consume `facts["ball_in_court"] = True`, `facts["importance"] = 0.9`, etc., so
the existing ForwardChainer gets sharper with zero engine change.

Foundation of INTELLIGENCE_2ND_BRAIN_PLAN.md (founder + sales). Generic and
module-agnostic — sales/founder only DECLARE which signals they consume.
"""
