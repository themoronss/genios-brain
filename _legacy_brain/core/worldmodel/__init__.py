"""g-i-3 Worldmodel ("Company Mind").

3 layers added on top of the g-i-2 graph (no fork):
1. Extract + Resolve  — MemoryItem -> entities -> deduped nodes (reversible merge)
2. Temporal           — as_of / diff / history queries + snapshot policy
3. Learning loop      — Correction -> Bayesian update -> rule-gap + candidate-edge propose-confirm
"""
