# Shape and identity

`ExecutionObject` contains tenant/source lineage, goal/context, typed actions and dependencies,
assignment, communication plan, escalation ladder, time boundaries, hashes and schema version.

`execution_id` is content-addressed from organization, authoritative decision and plan. Routing is
excluded deliberately so reassignment does not fork one business commitment into two identities.
