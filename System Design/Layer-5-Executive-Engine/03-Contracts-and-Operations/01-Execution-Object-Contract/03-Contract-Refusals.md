# Contract refusals

Construction refuses invalid dependencies, duplicate action identifiers, invalid clocks, forbidden
external effects, missing required authority and inconsistent communication/escalation data.

A refusal is cheaper and safer than persisting an execution that later components have to guess
how to interpret.
