# Forbidden model authority

A model may not classify action kind, resolve an owner, set a deadline, reorder dependencies,
choose an escalation, approve a guard verdict, transition state or label an outcome.

Those decisions are fixed logic over frozen input and explicit time.
