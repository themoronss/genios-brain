# Reminder and escalation races

Due rows are candidates, not permission. The live guard runs after retrieval. Escalation rungs and
synthetic delivery identities make repeated processing idempotent.

Blocked dependency work is withheld. A reporting-line change resolves the new recipient at the
last safe moment.
