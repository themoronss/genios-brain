# Outcome taxonomy

`succeeded` is positive. `expired_untouched`, `expired_in_progress` and
`cancelled_by_human` are negative. `completed_unproven`, `cancelled_by_world` and
`cancelled_by_system` remain neutral/unproven.

The distinctions prevent activity, silence or system intervention from being trained as business
success.
