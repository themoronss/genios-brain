# Output, edge cases and gaps

**Output:** a primary `Destination` plus bounded fallback sequence.

**Edge cases / honest gap:** No destination is invented from an email/handle string. If no eligible route exists, delivery is held/suppressed with a reason. Real registration and credentials are deployment concerns.
