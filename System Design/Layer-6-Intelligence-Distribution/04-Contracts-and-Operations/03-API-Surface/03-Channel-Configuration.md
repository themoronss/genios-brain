# Channel configuration

Channel routes list, create/update, delete and test tenant destinations, including dedicated Slack
operations and generic supported channels.

Secrets/URLs are tenant-owned configuration. A test endpoint proves adapter reachability only; it
does not bypass normal admission for production work.
