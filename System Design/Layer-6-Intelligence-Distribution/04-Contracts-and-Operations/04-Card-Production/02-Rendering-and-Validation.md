# Rendering and validation

A bounded temperature-zero model call may improve copy only. Deterministic validation checks the
rendered result against the allowed fact corpus, with a safe deterministic fallback.

Rendering cannot alter priority, recipient, channel, policy or execution state.
