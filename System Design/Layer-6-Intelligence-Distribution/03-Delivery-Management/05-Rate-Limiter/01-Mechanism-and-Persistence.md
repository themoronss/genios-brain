# Mechanism and persistence

Daily budget determines whether another item may be routed; burst state produces a timing deferral. Quiet hours and busy presence compose with—not replace—rate controls.

All writes remain organization-scoped and reason-coded so retry/recovery cannot erase the original
attempt.
