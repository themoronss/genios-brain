# Edge cases and gaps

The authoritative implementation is database-backed and tenant/seat scoped. There is no Redis/distributed counter acceleration, so production-volume contention needs proof.

Transport evidence is never upgraded into business success without the Layer 5 outcome seam.
