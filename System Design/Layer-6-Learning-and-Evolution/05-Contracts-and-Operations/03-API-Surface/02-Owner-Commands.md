# Owner commands

Owner-only endpoints create explicit bounded memory, review a LearningObject, roll back published
learning and update policy. Review/rollback delegate to transition/store authority inside a
transaction.

Payload tenant ids cannot override authenticated organization context.
