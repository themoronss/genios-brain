# Policy validation

Policy updates bound counts, days, basis-point thresholds and TTL. Knowledge Suggestion remains a
required human-review target even when clients submit target lists.

Invalid target/state/value combinations return explicit errors; the API cannot invent a publisher.
