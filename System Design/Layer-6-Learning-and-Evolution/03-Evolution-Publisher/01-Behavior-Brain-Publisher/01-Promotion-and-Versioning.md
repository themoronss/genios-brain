# Promotion and versioning

Publishes a versioned active row per tenant+brain+subject after automatic or configured review path. A newer row supersedes the prior active row and transitions its source object.

Publication is invoked only after the legal validation/governance lifecycle. The publisher cannot
accept an arbitrary target or bypass transition audit.
