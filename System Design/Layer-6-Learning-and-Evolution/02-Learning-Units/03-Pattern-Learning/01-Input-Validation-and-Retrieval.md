# Input, Validator and Retriever

## Input / Validator

Explicit temporary-memory events are excluded. Events must already be normalized and carry pattern key, kind, tenant, time and event id.

## Retriever

No prose semantics are guessed. The selector supplies the bounded enterprise-event cohort.

The unit never crosses tenant boundaries and never replaces missing source evidence with generated
facts.
