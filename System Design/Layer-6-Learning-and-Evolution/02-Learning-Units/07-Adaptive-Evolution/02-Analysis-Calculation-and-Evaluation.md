# Analyzer, Calculator and Evaluator

## Analyzer / Calculator

The first `preference:` subject prefix becomes `adaptive:` and target becomes Adaptive. The unit
does not recalculate support or turn temporary memory into a preference. Winner value, conflict and
all source evidence remain those of the explicit parent preference.

## Evaluator

Unit 11 and governance run again, so independent repetition, conflict, confidence, freshness,
subject visibility and constrained-ACL review still apply. No opaque personalization model or
current wall-clock state selects a winner.

The resulting object is content addressed and stable across fact order and evaluation-clock-only
changes.
