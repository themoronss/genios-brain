# Analyzer, Calculator and Evaluator

## Analyzer / Calculator

One-shot memory receives explicit evidence; governance applies the separate Runtime exception and maximum TTL (default 720 hours).

## Evaluator

It cannot follow the permanent-brain promotion path. Expiry is database-authoritative and runs before weekly claims.

All counts, thresholds and rates are deterministic integers with explicit observation time.
