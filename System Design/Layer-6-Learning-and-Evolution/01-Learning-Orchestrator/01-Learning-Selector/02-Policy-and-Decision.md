# Policy and decision

The default window is bounded (28 days). Queries preserve source identity/lineage and distinguish explicit feedback, outcomes, transport facts and normalized events.

The same batch, policy and evaluation time produce the same decision. Every hold/reject/review path
is reason-coded.
