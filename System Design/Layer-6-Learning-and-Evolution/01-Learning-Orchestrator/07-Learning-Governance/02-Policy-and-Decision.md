# Policy and decision

Governance can disable learning, block subject/target, require human review or reject an excessive temporary TTL. It never upgrades weak validation.

The same batch, policy and evaluation time produce the same decision. Every hold/reject/review path
is reason-coded.
