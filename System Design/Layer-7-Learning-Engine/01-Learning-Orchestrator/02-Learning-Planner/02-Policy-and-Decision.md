# Policy and decision

Ten analysis units run in fixed order; Unit 11 validation follows each candidate. Inapplicable units return no object rather than emitting low-quality placeholders.

The same batch, policy and evaluation time produce the same decision. Every hold/reject/review path
is reason-coded.
