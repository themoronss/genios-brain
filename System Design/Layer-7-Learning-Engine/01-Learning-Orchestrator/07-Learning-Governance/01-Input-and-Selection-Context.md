# Input and selection context

validated LearningObject plus tenant enablement, blocked subjects/targets, review rules and TTL ceiling.

All data is tenant-scoped, durable and evaluated at an explicit time. The orchestrator does not
quietly retrieve live state after a LearningObject hash is constructed.
