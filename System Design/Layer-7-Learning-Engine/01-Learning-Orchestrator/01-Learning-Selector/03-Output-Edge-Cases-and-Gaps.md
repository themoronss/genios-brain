# Output, edge cases and gaps

**Output:** a canonical `LearningBatch` consumed by the fixed analysis plan.

**Edge case / honest gap:** Selector quality cannot exceed upstream normalization. It does not ask an LLM to interpret raw free text at scoring time.
