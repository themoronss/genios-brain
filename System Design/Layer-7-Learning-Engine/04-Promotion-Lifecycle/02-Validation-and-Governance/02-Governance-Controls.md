# Governance controls

After validation, tenant policy may disable learning, block subject prefixes, require human review
for specified targets or bound Runtime TTL. Default human-review targets include Organization and
Knowledge Suggestion.

High confidence never overrides governance.
