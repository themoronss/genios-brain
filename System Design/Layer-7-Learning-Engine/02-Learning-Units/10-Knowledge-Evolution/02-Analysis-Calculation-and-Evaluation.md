# Analyzer, Calculator and Evaluator

## Analyzer / Calculator

A suggestion is created only when success is below 4000 basis points. The calculation is deterministic and names `sustained_low_outcome_rate`.

## Evaluator

Default governance requires human review. No confidence level can authorize Expert Brain mutation.

All counts, thresholds and rates are deterministic integers with explicit observation time.
