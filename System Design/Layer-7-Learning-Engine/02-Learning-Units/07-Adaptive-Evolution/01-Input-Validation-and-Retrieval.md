# Input, Validator and Retriever

## Input / Validator

Only the closed adaptive-category set participates, after explicit structured Preference Learning input exists.

## Retriever

Evidence and source lineage are inherited from the parent proposal without rereading mutable input.

The unit never crosses tenant boundaries and never replaces missing source evidence with generated
facts.
