# Promotion and versioning

Default policy routes Organization proposals through human review. Approval promotes/publishes a versioned tenant-scoped entry; newer approved evidence supersedes the prior version.

Publication is invoked only after the legal validation/governance lifecycle. The publisher cannot
accept an arbitrary target or bypass transition audit.
