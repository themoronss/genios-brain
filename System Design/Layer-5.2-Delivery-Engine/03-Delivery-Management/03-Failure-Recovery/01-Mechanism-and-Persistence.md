# Mechanism and persistence

The failed row remains audit evidence. Before fallback enqueue/send, the parent execution authority is re-proved and destination eligibility is recalculated.

All writes remain organization-scoped and reason-coded so retry/recovery cannot erase the original
attempt.
