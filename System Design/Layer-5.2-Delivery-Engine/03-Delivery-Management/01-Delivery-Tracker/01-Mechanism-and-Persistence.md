# Mechanism and persistence

The outbox is the one transport ledger. Queue, defer, suppress, cancel, sent, retry and terminal failure facts are stored; `results.py` projects public typed values without a second mutable truth.

All writes remain organization-scoped and reason-coded so retry/recovery cannot erase the original
attempt.
