# Rules and decision

Layer 5 decides chat/digest/in-app intent. Delivery chooses only among compatible destinations and formats; it cannot upgrade a non-interruptive plan to intrusive transport.

The decision path is deterministic and emits a reason that can be stored and explained. A model is
not an alternate policy engine.
