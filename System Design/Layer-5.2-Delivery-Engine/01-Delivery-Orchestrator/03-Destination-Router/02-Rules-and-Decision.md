# Rules and decision

Candidates are tenant-scoped, filtered by channel/eligibility and sorted by deterministic priority with a stable tie-break. Physical database order is not policy.

The decision path is deterministic and emits a reason that can be stored and explained. A model is
not an alternate policy engine.
