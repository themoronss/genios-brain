# Rules and decision

Quiet hours, busy state and burst constraints are evaluated independently. Non-intrusive pull/digest candidates are not delayed as though they wake a device.

The decision path is deterministic and emits a reason that can be stored and explained. A model is
not an alternate policy engine.
