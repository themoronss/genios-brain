# Verification limit

The local suite result proves deterministic behavior and faked adapter/storage scenarios. It does
not prove real Slack/Teams permissions, external webhook egress, provider rate limits or live
PostgreSQL claims.

Those checks are explicitly retained in the production runbook.
