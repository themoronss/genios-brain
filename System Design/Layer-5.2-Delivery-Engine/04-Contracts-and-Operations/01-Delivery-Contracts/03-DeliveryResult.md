# DeliveryResult

The result projects delivery identity, current status, reason, destination/channel, attempts,
deferrals and relevant clocks from the durable ledger.

It reports what transport knows. It does not claim the user read the content or that the business
execution succeeded.
