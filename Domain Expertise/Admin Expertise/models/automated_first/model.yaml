# Automated-First — model branch node.
#
# INHERITANCE: this file declares only what DIFFERS from its parent. Capability and object
# files are NOT copied into this branch; resolution is vertical -> model -> canonical.

identity:
  id: admin.model.automated_first
  name: Automated-First
  kind: model
  domain: admin
  parent: null
  version: 0.1.0
  status: draft
  stub: false

description: >
  Administration built around workflow automation and self-service, where a human touch is an
  exception rather than the path. The administrative skill shifts from doing the work to
  designing the exception route, because the exceptions are what remain.

characteristics:
  typical_cycle_days: 1
  committee_size: "1-2 approvers, automated below threshold"
  decision_style: "rules first, human on exception only"
  primary_channels: [portal, chat, api]

capabilities:
  emphasises:
    - admin.admin_operations.process_improvement
    - admin.admin_operations.sop_management
    - admin.admin_operations.admin_analytics

metadata:
  owner: Admin
  created_by: ai
  reviewed_by: ""
  last_updated: "2026-08-08"
  review_status: unreviewed
  confidence: experimental
