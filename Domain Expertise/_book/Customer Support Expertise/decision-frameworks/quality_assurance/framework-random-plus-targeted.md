<!-- GENERATED by _tools/render.py — edit the YAML, not this file. -->

# Framework random plus targeted

`customer_support.df.quality_assurance.framework_random_plus_targeted` · v1.0.0 · **draft** · scope **capability** · kind **decision_framework**

> Random-plus-targeted sampling — an estimation bucket and an investigation bucket, reported separately.

Separating them is what lets a programme both answer "how is the queue doing" and "what went wrong last Tuesday" without either question corrupting the other.


## Criteria

| Criterion | Weight | Disqualifying | Reads | Notes |
|---|---|---|---|---|
| Framework random plus targeted |  |  |  | Random-plus-targeted sampling — an estimation bucket and an investigation bucket, reported separately. |


## How to apply

- Random-plus-targeted sampling — an estimation bucket and an investigation bucket, reported separately.
- Separating them is what lets a programme both answer "how is the queue doing" and "what went wrong last Tuesday" without either question corrupting the other.

## Metadata

owner **Customer Support** · updated 2026-08-08 · review **unreviewed** · confidence **provisional**


> Extracted from an inline capability list, where it was a single statement plus a rationale. The structured body below therefore has one entry — it is the whole of what was authored, not a summary of something longer. Decomposing it into real steps, dimensions or criteria is the promotion work.
