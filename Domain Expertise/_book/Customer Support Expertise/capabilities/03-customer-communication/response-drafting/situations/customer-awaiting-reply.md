<!-- GENERATED by _tools/render.py — edit the YAML, not this file. -->

# Customer Awaiting Reply

`customer_support.sit.customer_awaiting_reply` · v1.0.0 · **draft**

Owned by `customer_support.customer_communication.response_drafting`


> A customer has written and the next move is ours. Nothing else is known — not whether a clock is running, not whether the question is hard, not whether anyone has started on it. The only fact in evidence is that a human asked something and the silence since then belongs to us. That is enough to act on, and treating it as insufficient is how threads rot: teams wait for the ticket record to catch up before replying, and the customer experiences the lag as being ignored rather than as being processed. The expert reading is that the reply owed here is not necessarily the answer — an acknowledgement that names what was asked, what happens next and who owns it discharges most of the obligation, and can be written before the diagnosis exists.


## Layer 2 binding

The join that makes this situation reachable. An unbound situation never compiles, and nothing errors when it doesn't.

- `unanswered_email`


Narrowed by `thread.ball_in_court = us`


Scope `person`


## Object load-set

- `customer_support.obj.core.ticket`
- `customer_support.obj.core.requester`
- `customer_support.obj.core.conversation`


### Optional

- `customer_support.obj.core.knowledge_article`
- `customer_support.obj.core.customer_sentiment`
- `customer_support.obj.core.entitlement`


### Never load

- `customer_support.obj.core.incident`
- `customer_support.obj.core.escalation`


## Also serves

- `customer_support.entitlement_and_sla.commitment_tracking`
- `customer_support.entitlement_and_sla.breach_prevention`


## Signals of progress

- `followup_sent`
- `first_response_sent`
- `callback_promised`


## Signals of decay

- `angry_language`
- `escalation_requested`
- `cancellation_threat`

## Metadata

owner **Customer Support** · updated 2026-08-08 · review **unreviewed** · confidence **provisional**


> signals_of_progress and signals_of_decay mix real and planned observation kinds. followup_sent is in the substrate today; first_response_sent, callback_promised, angry_language, escalation_requested and cancellation_threat are support-native kinds in planned_substrate.obs_kinds and will not fire until a support pack emits them. These lists are not validated, so the mixture is legal — recording it here so nobody later reads the list as evidence of coverage.
The honest limitation of this situation is that it fires on elapsed silence and knows nothing about what was promised. It cannot distinguish a customer owed a four-hour first response from one who asked a nice-to-have question on a Friday. That distinction is sla-breach-imminent.yaml, and it does not route yet.
