<!-- GENERATED by _tools/render.py — edit the YAML, not this file. -->

# Major Incident Declared

`customer_support.sit.major_incident_declared` · v1.0.0 · **draft**

Owned by `customer_support.escalation_and_incident.incident_management`


> A fault has stopped being one customer's problem and has been declared as such. The declaration is the event — not the fault, which was already happening, and not the ticket volume, which arrives late and undercounts badly because the customers who did not write in have already formed an opinion. From the moment of declaration the unit of work is the incident and not the ticket: there is a commander who coordinates rather than debugs, one status narrative rather than many replies, and a standing assumption that anything the individual-ticket machinery wants to do right now is probably wrong. The situation stays live through the customer-side cleanup, which routinely outlasts the technical restoration by days and is where the relationship damage is actually repaired or confirmed.


## Layer 2 binding

The join that makes this situation reachable. An unbound situation never compiles, and nothing errors when it doesn't.

_none_


Scope `account`


## Object load-set

- `customer_support.obj.core.incident`
- `customer_support.obj.core.issue`
- `customer_support.obj.core.commitment`
- `customer_support.obj.core.product_area`


### Optional

- `customer_support.obj.core.ticket`
- `customer_support.obj.core.sla_target`
- `customer_support.obj.core.customer_account`


### Never load

- `customer_support.obj.core.requester`
- `customer_support.obj.core.customer_sentiment`
- `customer_support.obj.core.knowledge_article`


## Also serves

- `customer_support.escalation_and_incident.major_incident_communication`
- `customer_support.support_operations.queue_management`
- `customer_support.escalation_and_incident.engineering_handoff`


## Signals of progress

- `incident_resolved`
- `root_cause_identified`
- `workaround_provided`
- `followup_sent`


## Signals of decay

- `ticket_created`
- `escalation_requested`
- `cancellation_threat`
- `angry_language`

## Metadata

owner **Customer Support** · updated 2026-08-08 · review **unreviewed** · confidence **provisional**


> Almost every string in signals_of_progress and signals_of_decay is a support-native obs kind from planned_substrate (incident_resolved, root_cause_identified, workaround_provided, ticket_created, escalation_requested, cancellation_threat, angry_language) and is emitted by nothing today; only followup_sent is real substrate. The schema does not validate these strings, so the file loads — a reader should not read that as the situation being live. ticket_created appears under decay, which looks backwards and is not. Once an incident has been declared, new tickets are the lagging measure of how badly the public communication is working: every ticket after the status page updated is a customer who did not find it, or did not believe it. typical_duration_days is 3 rather than the few hours a technical restoration takes, because the situation is not over when the fault stops. It is over when the affected accounts have been told what happened, and the gap between those two moments is where this capability either keeps the relationship or loses it.
