<!-- GENERATED by _tools/render.py — edit the YAML, not this file. -->

# Bug Awaiting Engineering

`customer_support.sit.bug_awaiting_engineering` · v1.0.0 · **draft**

Owned by `customer_support.escalation_and_incident.engineering_handoff`


> A defect has been filed and the customer is waiting on somebody who does not work in support. Support has done its part — reproduced it, written it up, handed it over — and has now entered the state it is worst at: owning a relationship while owning none of the work. The ticket has no next action an agent can take, which is precisely why it stops appearing in anyone's day. Frequently a workaround exists, which makes it worse rather than better: the ticket is marked resolved on the strength of it, the clock stops, the queue reports health, and a customer is left performing a manual step every morning against a defect nobody is telling them about. The situation is live for as long as the customer's problem is real, regardless of what state their ticket is in.


## Layer 2 binding

The join that makes this situation reachable. An unbound situation never compiles, and nothing errors when it doesn't.

_none_


Scope `person`


## Object load-set

- `customer_support.obj.core.bug_report`
- `customer_support.obj.core.issue`
- `customer_support.obj.core.ticket`
- `customer_support.obj.core.commitment`
- `customer_support.obj.core.workaround`


### Optional

- `customer_support.obj.core.root_cause`
- `customer_support.obj.core.product_area`
- `customer_support.obj.core.requester`


### Never load

- `customer_support.obj.core.sla_target`
- `customer_support.obj.core.resolution`


## Also serves

- `customer_support.customer_communication.proactive_status_updates`
- `customer_support.diagnosis_and_resolution.workaround_management`
- `customer_support.voice_of_customer.product_feedback_routing`


## Signals of progress

- `followup_sent`
- `workaround_provided`
- `root_cause_identified`
- `bug_fixed`


## Signals of decay

- `escalation_requested`
- `cancellation_threat`
- `angry_language`
- `ticket_reopened`

## Metadata

owner **Customer Support** · updated 2026-08-08 · review **unreviewed** · confidence **provisional**


> Only followup_sent is a real substrate obs kind; workaround_provided, root_cause_identified, bug_fixed, escalation_requested, cancellation_threat, angry_language and ticket_reopened are support-native names from planned_substrate that nothing emits today. They are unvalidated free-form strings, so this file loads and does not run. followup_sent is listed first in signals_of_progress on purpose and is the most important entry in either list. It is the only one support controls. Every other progress signal belongs to engineering, and a situation whose progress measures are all owned by another team is one that support will conclude it cannot influence — which is the belief that produces the silence in the first place. typical_duration_days is 45, which is long enough to look like an error. It is not. It is set against the customer's problem rather than the ticket's status, and defect fix cycles are measured in weeks in most organisations; setting it to the ticket lifetime would mark this situation complete at the moment the workaround shipped, which is the exact failure that workaround_only exists to catch.
