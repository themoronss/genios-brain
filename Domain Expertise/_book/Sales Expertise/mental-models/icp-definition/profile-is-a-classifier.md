<!-- GENERATED by _tools/render.py — edit the YAML, not this file. -->

# An ICP is a classifier and every classifier has two error types. Narro…

`sales.mm.icp_definition.profile_is_a_classifier` · v0.1.0 · **draft** · scope **capability** · kind **mental_model**

> An ICP is a classifier and every classifier has two error types. Narrowing it trades wasted effort for missed accounts; widening it trades the reverse.
 Which error costs more is a business decision that changes with capacity — a team with spare capacity and a full quarter should run wide; a team at capacity should run narrow. Arguing about the profile's width without naming which error you are cu


## Dimensions

| Dimension | Asks | Weak signal | Strong signal | Objects |
|---|---|---|---|---|
| An ICP is a classifier and every classifier has two error types. Narro… | Which error costs more is a business decision that changes with capacity — a team with spare capacity and a full quarter should run wide; a team at capacity should run narrow. Arguing about the profile's width without naming which error you are currently paying for is how these debates run for a year without resolving.  |  |  |  |


## How to apply

- Which error costs more is a business decision that changes with capacity — a team with spare capacity and a full quarter should run wide; a team at capacity should run narrow. Arguing about the profile's width without naming which error you are currently paying for is how these debates run for a year without resolving.


## Metadata

owner **Sales** · updated 2026-08-08 · review **unreviewed** · confidence **provisional** · completeness **skeleton**


> Extracted from an inline list on sales.market_and_targeting.icp_definition. Now addressable: it has an id and a version, so Layer 6 can raise a suggestion against it. The body is a skeleton.
