<!-- GENERATED by _tools/render.py — edit the YAML, not this file. -->

# Win–loss–churn cohort comparison — three cohorts, dimensions ranked by…

`sales.df.icp_definition.tri_cohort` · v0.1.0 · **draft** · scope **capability** · kind **decision_framework**

> Win–loss–churn cohort comparison — three cohorts, dimensions ranked by separation between them rather than by prevalence within any one.
 It is the only structure in common use that can detect the fast-closing, fast-churning segment, because that segment looks identical to the ideal one in a win-loss study.


## Criteria

| Criterion | Weight | Disqualifying | Reads | Notes |
|---|---|---|---|---|
| Win–loss–churn cohort comparison — three cohorts, dimensions ranked by… |  |  |  | It is the only structure in common use that can detect the fast-closing, fast-churning segment, because that segment looks identical to the ideal one in a win-loss study.  |


## How to apply

- It is the only structure in common use that can detect the fast-closing, fast-churning segment, because that segment looks identical to the ideal one in a win-loss study.


## Metadata

owner **Sales** · updated 2026-08-08 · review **unreviewed** · confidence **provisional** · completeness **skeleton**


> Extracted from an inline list on sales.market_and_targeting.icp_definition. Now addressable: it has an id and a version, so Layer 6 can raise a suggestion against it. The body is a skeleton.
