# Extracted from the capability that used to carry this inline. Scoped to
# customer_support.escalation_and_incident.escalation_management — exactly one capability references it. Promote to
# playbooks/core/ the moment a second one does; validate.py enforces that
# from the reference graph rather than from intent.

identity:
  id: customer_support.pb.escalation_management.exit_condition
  name: Exit condition
  kind: playbook
  domain: customer_support
  scope: capability
  owner_capability: customer_support.escalation_and_incident.escalation_management
  subdomain: escalation_and_incident
  version: 1.0.0
  status: draft
purpose:
  statement: 'State the de-escalation condition at the moment of handoff, not later. Write down what
    would make this an ordinary ticket again.


    An exit condition authored at the start is a description of the problem; authored at the end it
    is a justification for closing the flag. Only the first one is useful, and only the first one keeps
    the escalated pool honest.'
when_to_use:
  do_not_use_when:
  - The preceding step in this capability has not produced its artefact yet.
steps:
- order: 1
  name: State the de-escalation condition at the moment of handoff, not later. Write down what would
    make this an ordinary ticket again.
  actor: human
  description: An exit condition authored at the start is a description of the problem; authored at
    the end it is a justification for closing the flag. Only the first one is useful, and only the first
    one keeps the escalated pool honest.
metadata:
  owner: Customer Support
  created_by: ai
  reviewed_by: ''
  last_updated: '2026-08-08'
  review_status: unreviewed
  confidence: provisional
  notes: Extracted from an inline capability list, where it was a single statement plus a rationale.
    The structured body below therefore has one entry — it is the whole of what was authored, not a
    summary of something longer. Decomposing it into real steps, dimensions or criteria is the promotion
    work.
