# Extracted from the capability that used to carry this inline. Scoped to
# customer_support.support_management.quality_assurance — exactly one capability references it. Promote to
# playbooks/core/ the moment a second one does; validate.py enforces that
# from the reference graph rather than from intent.

identity:
  id: customer_support.pb.quality_assurance.evidence_anchored_scoring
  name: Evidence anchored scoring
  kind: playbook
  domain: customer_support
  scope: capability
  owner_capability: customer_support.support_management.quality_assurance
  subdomain: support_management
  version: 1.0.0
  status: draft
purpose:
  statement: 'Every mark below full quotes the specific moment in the transcript that produced it. No
    quote, no deduction.


    The quote is what makes a score coachable and appealable. Without it the agent can only accept the
    mark or resent it, and the reviewer never has to test whether the judgement survives being written
    down.'
when_to_use:
  do_not_use_when:
  - The preceding step in this capability has not produced its artefact yet.
steps:
- order: 1
  name: Every mark below full quotes the specific moment in the transcript that produced it. No quote,
    no deduction.
  actor: human
  description: The quote is what makes a score coachable and appealable. Without it the agent can only
    accept the mark or resent it, and the reviewer never has to test whether the judgement survives
    being written down.
metadata:
  owner: Customer Support
  created_by: ai
  reviewed_by: ''
  last_updated: '2026-08-08'
  review_status: unreviewed
  confidence: provisional
  notes: Extracted from an inline capability list, where it was a single statement plus a rationale.
    The structured body below therefore has one entry — it is the whole of what was authored, not a
    summary of something longer. Decomposing it into real steps, dimensions or criteria is the promotion
    work.
