# Extracted from the capability that used to carry this inline. Scoped to
# customer_support.intake_and_triage.ticket_triage — exactly one capability references it. Promote to
# playbooks/core/ the moment a second one does; validate.py enforces that
# from the reference graph rather than from intent.

identity:
  id: customer_support.pb.ticket_triage.hand_off_with_the_assumption
  name: Hand off with the assumption
  kind: playbook
  domain: customer_support
  scope: capability
  owner_capability: customer_support.intake_and_triage.ticket_triage
  subdomain: intake_and_triage
  version: 1.0.0
  status: draft
purpose:
  statement: 'Write the assumption, not the conclusion, into the triage note.


    The receiving agent can falsify an assumption in seconds and cannot falsify a verdict at all. Notes
    that record only the verdict are why misroutes persist through three owners.'
when_to_use:
  do_not_use_when:
  - The preceding step in this capability has not produced its artefact yet.
steps:
- order: 1
  name: Write the assumption, not the conclusion, into the triage note.
  actor: human
  description: The receiving agent can falsify an assumption in seconds and cannot falsify a verdict
    at all. Notes that record only the verdict are why misroutes persist through three owners.
metadata:
  owner: Customer Support
  created_by: ai
  reviewed_by: ''
  last_updated: '2026-08-08'
  review_status: unreviewed
  confidence: provisional
  notes: Extracted from an inline capability list, where it was a single statement plus a rationale.
    The structured body below therefore has one entry — it is the whole of what was authored, not a
    summary of something longer. Decomposing it into real steps, dimensions or criteria is the promotion
    work.
