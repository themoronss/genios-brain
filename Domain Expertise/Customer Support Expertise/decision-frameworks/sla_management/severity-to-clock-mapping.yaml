# Extracted from the capability that used to carry this inline. Scoped to
# customer_support.entitlement_and_sla.sla_management — exactly one capability references it. Promote to
# decision-frameworks/core/ the moment a second one does; validate.py enforces that
# from the reference graph rather than from intent.

identity:
  id: customer_support.df.sla_management.severity_to_clock_mapping
  name: Severity to clock mapping
  kind: decision_framework
  domain: customer_support
  scope: capability
  owner_capability: customer_support.entitlement_and_sla.sla_management
  subdomain: entitlement_and_sla
  version: 1.0.0
  status: draft
purpose:
  statement: 'Severity-to-clock mapping — severity selects the clock, so re-examine severity before
    renegotiating a deadline.


    It keeps the negotiation on the object that should carry it. Changing severity is a reviewable decision
    with a rationale; changing a deadline is a favour, and favours become precedent.'
criteria:
- name: Severity to clock mapping
  description: Severity-to-clock mapping — severity selects the clock, so re-examine severity before
    renegotiating a deadline.
how_to_apply:
- Severity-to-clock mapping — severity selects the clock, so re-examine severity before renegotiating
  a deadline.
- It keeps the negotiation on the object that should carry it. Changing severity is a reviewable decision
  with a rationale; changing a deadline is a favour, and favours become precedent.
metadata:
  owner: Customer Support
  created_by: ai
  reviewed_by: ''
  last_updated: '2026-08-08'
  review_status: unreviewed
  confidence: provisional
  notes: Extracted from an inline capability list, where it was a single statement plus a rationale.
    The structured body below therefore has one entry — it is the whole of what was authored, not a
    summary of something longer. Decomposing it into real steps, dimensions or criteria is the promotion
    work.
