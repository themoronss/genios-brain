# Extracted from the capability that used to carry this inline. Scoped to
# customer_support.support_management.quality_assurance — exactly one capability references it. Promote to
# mental-models/core/ the moment a second one does; validate.py enforces that
# from the reference graph rather than from intent.

identity:
  id: customer_support.mm.quality_assurance.teaching_first
  name: Teaching first
  kind: mental_model
  domain: customer_support
  scope: capability
  owner_capability: customer_support.support_management.quality_assurance
  subdomain: support_management
  version: 1.0.0
  status: draft
purpose:
  statement: QA is a teaching instrument that produces a number as a side effect. Where an organisation
    treats it as a measurement instrument that produces teaching as a side effect, everything else in
    this file fails in a predictable order — sampling first, then honesty, then the rubric.
dimensions:
- name: Teaching first
  asks: QA is a teaching instrument that produces a number as a side effect. Where an organisation treats
    it as a measurement instrument that produces teaching as a side effect, everything else in this
    file fails in a predictable order — sampling first, then honesty, then the rubric.
how_to_apply:
- QA is a teaching instrument that produces a number as a side effect. Where an organisation treats
  it as a measurement instrument that produces teaching as a side effect, everything else in this file
  fails in a predictable order — sampling first, then honesty, then the rubric.
metadata:
  owner: Customer Support
  created_by: ai
  reviewed_by: ''
  last_updated: '2026-08-08'
  review_status: unreviewed
  confidence: provisional
  notes: Extracted from an inline capability list, where it was a single statement plus a rationale.
    The structured body below therefore has one entry — it is the whole of what was authored, not a
    summary of something longer. Decomposing it into real steps, dimensions or criteria is the promotion
    work.
