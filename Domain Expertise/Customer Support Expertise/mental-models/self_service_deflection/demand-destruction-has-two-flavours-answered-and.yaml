# Extracted from the capability that used to carry this inline. Scoped to
# customer_support.knowledge_and_deflection.self_service_deflection — exactly one capability references it. Promote to
# mental-models/core/ the moment a second one does; validate.py enforces that
# from the reference graph rather than from intent.

identity:
  id: customer_support.mm.self_service_deflection.demand_destruction_has_two_flavours_answered_and
  name: Demand destruction has two flavours answered and
  kind: mental_model
  domain: customer_support
  scope: capability
  owner_capability: customer_support.knowledge_and_deflection.self_service_deflection
  subdomain: knowledge_and_deflection
  version: 1.0.0
  status: draft
purpose:
  statement: 'Demand destruction has two flavours — answered and abandoned — and from the ticket queue
    they are indistinguishable.


    This is the whole reason the capability exists. Any measurement design that cannot separate them
    will, given enough optimisation pressure, converge on the wrong one because it is cheaper.'
dimensions:
- name: Demand destruction has two flavours answered and
  asks: Demand destruction has two flavours — answered and abandoned — and from the ticket queue they
    are indistinguishable.
how_to_apply:
- Demand destruction has two flavours — answered and abandoned — and from the ticket queue they are
  indistinguishable.
- This is the whole reason the capability exists. Any measurement design that cannot separate them will,
  given enough optimisation pressure, converge on the wrong one because it is cheaper.
metadata:
  owner: Customer Support
  created_by: ai
  reviewed_by: ''
  last_updated: '2026-08-08'
  review_status: unreviewed
  confidence: provisional
  notes: Extracted from an inline capability list, where it was a single statement plus a rationale.
    The structured body below therefore has one entry — it is the whole of what was authored, not a
    summary of something longer. Decomposing it into real steps, dimensions or criteria is the promotion
    work.
